# MiniJava AST Generator using JFLEX & JavaCup

### Run using the following commands:

>`java -jar java-cup-11a.jar -parser MiniJavaParser -symbols SymTable parser_src.cup`
>
>`jflex lexer_src.flex`
>
>`javac Main.java`
>
>`java Main filename.java`

Tested on the cases in the `/tests` folder
